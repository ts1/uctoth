#!/usr/bin/env coffee

require './rejection_handler'
Book = require './book'
{ decode } = require './encode'

do ->
  book = new Book 'book.db', readonly: true
  await book.init()
  await book.dump_nodes (data) ->
    process.stdout.write "#{data.code} #{data.outcome}\n"
